import styled from 'styled-components';
import { Delete } from 'styled-icons/feather/Delete';
import { CheckboxChecked } from 'styled-icons/boxicons-regular/CheckboxChecked';
import { CheckboxUnchecked } from 'styled-icons/icomoon/CheckboxUnchecked';

export const DeleteIcon = styled(Delete)`
  color: black;
`;

export const Check = styled(CheckboxChecked)`
  color: black;
`;

export const Uncheck = styled(CheckboxUnchecked)`
  color: black;
`;
